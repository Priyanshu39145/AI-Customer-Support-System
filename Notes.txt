
🧠 AI-Powered Customer Support Backend — What I Have Built So Far

I started by designing the core structure of a real-world customer support system. First, I identified the main actors involved in the system: User, Agent, and Admin. Instead of creating separate tables for each, I used a single User entity with a role field (enum) to differentiate between them. This is a standard industry approach called role-based design, which keeps the system simple and scalable.

Next, I designed the core entities of the system: User, Ticket, and Message.

The User entity stores basic information like name, email, password, role, and timestamps. I ensured that the email is unique and important fields like name and password are non-null. I also used enums for roles (USER, AGENT, ADMIN) and stored them as strings in the database to avoid future issues.

The Ticket entity represents the main feature of the system. Each ticket contains a title, description, status (OPEN, IN_PROGRESS, RESOLVED), and priority (LOW, MEDIUM, HIGH). I added relationships to show that a ticket is created by a user and can be assigned to an agent. I also handled timestamps for tracking creation and updates. Additionally, I made sure that a ticket can exist without an assigned agent initially.


Then I designed the Message entity, which represents the conversation inside a ticket. Instead of building a direct messaging system (like WhatsApp), I followed a thread-based design, where each ticket acts as a conversation container. Messages belong to a specific ticket and are sent by a user (either the customer or the agent). This design is used in real support systems and allows features like conversation history and AI summarization.

I also implemented relationships between entities:
	•	A User can create multiple tickets
	•	A User (agent) can be assigned multiple tickets
	•	A Ticket can have multiple messages
	•	A Message belongs to one ticket and one sender

To make the system more complete, I added bi-directional relationships, meaning entities can reference each other in both directions. For example, a User can access their tickets, and a Ticket can access its messages.

However, this created a problem called infinite recursion when returning data through APIs. Because entities were linked in loops (User → Ticket → Message → User…), the system could crash or return huge nested JSON responses. To fix this, I used @JsonIgnore on certain fields to prevent unnecessary data from being serialized, ensuring clean and safe API responses.


I also made several important technical decisions:
	•	Used UUIDs for primary keys
	•	Used @CreationTimestamp and @UpdateTimestamp for automatic time tracking
	•	Used TEXT type for long descriptions
	•	Used @JoinColumn for clear database relationships
	•	Used Lombok to reduce boilerplate code

Overall, I have successfully built a solid backend domain model that reflects how real-world customer support systems work. The system now has proper structure, relationships, constraints, and is ready for the next phase, which includes repositories, service layer, and API development.


🚀 Current Status

I have completed:
	•	System design
	•	Entity modeling
	•	Database mapping
	•	Relationship handling
	•	Recursion problem handling

Next, I will move to:
👉 Creating repositories
👉 Building service layer (business logic)
👉 Implementing APIs


🧠 AI-Powered Customer Support Backend — Progress After Entity Design

After completing the entity design and database mapping, I moved to the backend implementation phase, where I started building the actual system behavior using repositories, service layers, and APIs.


🗄️ Repository Layer

I created repository interfaces for all core entities (User, Ticket, Message) using Spring Data JPA. These repositories extend JpaRepository, which provides built-in methods for common database operations like save, find, delete, and pagination.

For the Ticket entity, I added custom query methods to support filtering and pagination:
	•	Find tickets by status
	•	Find tickets by priority
	•	Find tickets by both status and priority

This allowed me to build flexible APIs without writing complex SQL queries.

⸻

⚙️ Service Layer (Business Logic)

I implemented a service layer to handle all business logic instead of putting logic inside controllers. This follows clean architecture principles and makes the code more maintainable and testable.

⸻

🎫 Create Ticket API

I built the first core API to create tickets.

Key logic implemented:
	•	Fetch user from database using userId
	•	Validate that the user exists
	•	Create a new ticket with:
	•	status = OPEN
	•	priority = user-provided or default (MEDIUM)
	•	assignedTo = null
	•	Save ticket to database

I used DTOs (Data Transfer Objects) to separate request and response from internal entities:
	•	CreateTicketRequestDTO for input
	•	CreateTicketResponseDTO for output

I also used ModelMapper to convert entity objects to DTOs and manually handled fields like IDs when needed.

⸻

📄 Get Tickets API (Pagination + Filtering)

Next, I implemented a scalable ticket retrieval API, which is a very important feature in real-world systems.

This API supports:
	•	Pagination using page and size
	•	Filtering by:
	•	status
	•	priority

I handled multiple cases in the service layer:
	•	Both status and priority present
	•	Only status
	•	Only priority
	•	No filters (return all tickets)

I used Spring’s Pageable and PageRequest to efficiently fetch paginated data from the database and mapped results to TicketResponseDTO.

This step helped me understand how to build APIs that can handle large datasets efficiently.

⸻

🧑‍💼 Assign Ticket API (Admin Feature)

I then implemented the feature where an admin assigns a ticket to an agent.

Key logic:
	•	Fetch ticket and agent from database
	•	Validate both exist
	•	Ensure the selected user has role = AGENT
	•	Assign the ticket to the agent
	•	Update ticket status from OPEN → IN_PROGRESS
	•	Return updated ticket as DTO

I also learned how to handle nested mapping issues when converting entities to DTOs. Since DTO should not expose full entities, I used fields like assignedToId instead of including the entire User object and manually mapped it.

This step introduced me to:
	•	Role validation
	•	Update operations
	•	Real-world business rules

⸻

🔄 Update Ticket Status API (Agent Feature)

Finally, I implemented the API to update ticket status, completing the ticket lifecycle.

Key logic:
	•	Fetch ticket from database
	•	Ensure the ticket is assigned to an agent
	•	Validate allowed status transitions:
	•	OPEN → IN_PROGRESS
	•	IN_PROGRESS → RESOLVED
	•	Prevent invalid transitions like:
	•	OPEN → RESOLVED
	•	RESOLVED → any other state
	•	Update status and return response

This introduced the concept of state machine validation, which is widely used in real systems to control workflow transitions.

⸻

⚠️ Important Backend Concepts Learned

During this phase, I learned several critical backend concepts:
	•	DTO pattern to avoid exposing entities directly
	•	ModelMapper limitations, especially with nested objects
	•	Manual mapping for fields like assignedToId
	•	Pagination using Pageable and PageRequest
	•	Dynamic filtering logic in service layer
	•	Role-based validation (Agent vs User)
	•	State transition validation (workflow control)
	•	Use of @Transactional and JPA dirty checking
	•	Avoiding infinite recursion in bi-directional relationships

⸻

🧠 System Design Understanding

At this stage, my project is no longer a simple CRUD application. It now behaves like a real-world support system with:
	•	Defined workflows
	•	Role-based actions
	•	Controlled state transitions
	•	Scalable data retrieval
	•	Clean layered architecture

⸻

🚀 Current Status

I have now completed:
	•	Repository layer
	•	Service layer with business logic
	•	Create Ticket API
	•	Get Tickets API with pagination and filtering
	•	Assign Ticket API (Admin flow)
	•	Update Ticket Status API (Agent flow)

⸻
Next I am going to set up JWT Authentication ---
First I added isEnabled attribute in User class to make sure to allow the user to log in ---
Then I created the SecurityConfig --- where I configured the SecurityFilterChain --- I disabled CORS and ensured Stateless authentication and initially made every request public
Then I configured PasswordEncoder using BCryptPasswordEncoder
Then I set up register API --- I made Register Request and ResponseDTOs
Then I made the AuthController and AuthService class where I made the register API which creates a new User with the info and an encrypted password stored in database ---

After this comes JWT Login ----
We have to make various changes ----
We first change the WebSecurityConfig --- disable csrf --- enable Stateless session management --- and then configure the requestmatchers ---
Then we change the User Entity --- make it implement the UserDetails interface --- and override the methods
Then we make a CustomUserDetailsService Method --- which has the loadByUsername method overridden
Then we make the AuthController and AuthService --- with the login method ---
Then we make the JWTAuthFilter --- see the class --- we add this filter with in the WebSecuityConfig
How the login works --- First we get the LoginRequestDTO --- where we have email and password ---
We delegate the login process to AuthenticationManager --- which uses our CustomUserDetailsService --- to fetch the user and authentication --- then we generate a JWT access token --- then return the LoginResponseDTO containing the token ---
Using the token when we try to approach a route the JWTAuthFilter validates and then allows us ----

Then we implement the Role Based Access Control ----
First inside SecurityConfig we authorize role based access using requestMatchers
And then using @EnableMethodSecurity on the SecurityConfig class we enable Method Security and then we apply @PreAuthorize("hasRole('ADMIN')") on specific methods for stronger Role Based Access Control

Here’s your next part of notes — written in the same clean, detailed, and structured way so you can directly add it to your file:

⸻

🧠 AI-Powered Customer Support Backend — Authentication & Security Phase

After successfully implementing JWT-based authentication and role-based access control, I moved to a more advanced and real-world feature: integrating OAuth2 (Google Login) along with JWT authentication. This significantly enhanced the system by allowing users to log in using either traditional email/password or their Google account.

⸻

🔐 OAuth2 Integration (Google Login)

To implement this, I first added the OAuth2 client dependency and configured Google credentials in the application properties. This allowed the backend to communicate with Google’s authentication system.

I then updated the SecurityConfig to include .oauth2Login(), where I defined custom success and failure handlers. Instead of using the default redirect-based behavior, I implemented a custom OAuth2SuccessHandler, which allowed me to fully control what happens after a successful Google login.

In the success handler, I extracted user details such as email, name, and provider-specific ID from the OAuth2User object. Then, I delegated the logic to the service layer to either register or log in the user.

⸻

🧠 Unified Authentication System (JWT + OAuth2)

One of the most important architectural decisions I made was to unify both authentication methods under JWT.

This means:
	•	Whether the user logs in via email/password or Google
	•	The backend always generates and returns a JWT token

This ensures that all APIs remain consistent and secure using a single authentication mechanism.

⸻

👤 Provider-Based User Management

To support OAuth2 properly, I extended the User entity by adding:
	•	providerType (EMAIL, GOOGLE)
	•	providerId (unique ID from OAuth provider)

This helps identify how a user registered and prevents duplicate accounts across different login methods.

In the service layer, I implemented logic to handle multiple scenarios:
	•	If user exists via provider → login directly
	•	If user exists via email → prevent duplicate account or link accounts
	•	If user does not exist → create a new user

This is a real-world approach used in production systems to handle multi-provider authentication.

⸻

🔄 OAuth2 Login Flow

The complete flow now works as follows:
	•	User initiates login via Google
	•	Google authenticates and sends user details to backend
	•	Backend extracts user information
	•	Checks if user exists in database
	•	If not → creates a new user
	•	Generates JWT token
	•	Returns token as JSON response

Unlike traditional OAuth2 setups, I avoided redirects and instead returned a clean JSON response, making it suitable for REST APIs and frontend integration.

⸻

⚠️ Fixing OAuth2 Redirect Behavior

Initially, I observed that unauthenticated API requests were automatically redirecting to the Google login page. This behavior is not suitable for backend APIs.

To fix this, I configured a custom authenticationEntryPoint, which ensures:
	•	Unauthorized requests return HTTP 401 (JSON response)
	•	No automatic redirection to OAuth login

This aligns the system with real-world API behavior.

⸻

🧠 Advanced Security Concepts Learned

During this phase, I learned several important concepts:
	•	Difference between authentication (who you are) and authorization (what you can do)
	•	How Spring Security internally uses AuthenticationManager and UserDetailsService
	•	How to build and integrate a custom JWT filter
	•	How to extract and validate tokens from request headers
	•	How OAuth2 providers supply user data via OAuth2User
	•	How to unify multiple login systems into a single JWT-based architecture
	•	Handling edge cases like duplicate users and provider conflicts
	•	Designing APIs to return JSON instead of redirects

⸻

🧠 System Design Upgrade


Then making the sendMessage API ---
Inside the Controller we get the ticketId and the MessageRequestDTO containing only the content --- We fetch the user using JWT --- from SecurityContextHolder and then inside the MessageService Method --- We check for three things---
If the user and the ticket are not null, if the user is either USER or AGENT, if the ticket belongs to the user or belongs to the AGENT. then we make the Message ---> save it and then send the MessageResponseDTO

Here are short, clean notes for the 3 APIs you built 👇

⸻

🧠 Message System APIs (Chat Feature)

I implemented a ticket-based messaging system, where each ticket acts as a conversation thread between the user and the assigned agent.

⸻

✉️ 1. Send Message API

POST /tickets/{ticketId}/messages

This API allows a user or agent to send a message inside a ticket.

Key logic:
	•	Extract current user from JWT (SecurityContext)
	•	Fetch ticket from database
	•	Validate access:
	•	Only ticket creator OR assigned agent can send messages
	•	Create a new Message entity with:
	•	content
	•	sender
	•	ticket
	•	If ticket is in OPEN state → update to IN_PROGRESS
	•	Save message and return MessageResponseDTO

⸻

📥 2. Get Messages API

GET /tickets/{ticketId}/messages

This API fetches the full conversation history of a ticket.

Key logic:
	•	Extract current user from JWT
	•	Validate access (same as send message)
	•	Fetch messages using:
	•	findByTicketOrderByCreatedAtAsc()
	•	Convert each Message → MessageResponseDTO
	•	Return list of messages (old → new order)

⸻

🎫 3. Get Ticket Detail API

GET /tickets/{ticketId}

This API returns complete ticket details along with chat history.

Key logic:
	•	Extract current user from JWT
	•	Validate access (creator OR assigned agent)
	•	Fetch ticket details
	•	Fetch messages separately (sorted)
	•	Map messages → MessageResponseDTO
	•	Return TicketDetailedResponseDTO containing:
	•	ticket info (title, description, status, priority)
	•	createdById, assignedToId
	•	list of messages

⸻

⚠️ Key Learnings
	•	Never return entities directly → always use DTOs
	•	Always validate access in secure APIs
	•	Use SecurityContext instead of passing userId
	•	Maintain proper ordering for chat systems
	•	Handle null cases (like unassigned tickets)
	•	Implement workflow logic (OPEN → IN_PROGRESS)

⸻

🚀 Current Status

You now have:
	•	✅ Ticket system
	•	✅ Assignment system
	•	✅ Status workflow
	•	✅ Chat system (send + fetch messages)
	•	✅ Ticket detail API (full view)

⸻

👉 Your backend is now feature-complete for a basic support system 💯

👤 User Dashboard API

GET /users/me/tickets
	•	Fetches tickets created by the logged-in user
	•	Uses JWT (SecurityContext) to identify user
	•	Supports:
	•	Pagination
	•	Filtering (status, priority)
	•	Returns Page<TicketResponseDTO>
	•	Helps users track their own tickets

⸻

🧑‍💼 Agent Dashboard API

GET /agents/me/tickets
	•	Fetches tickets assigned to the logged-in agent
	•	Validates role = AGENT
	•	Supports:
	•	Pagination
	•	Filtering (status, priority)
	•	Sorting by priority (HIGH → LOW)
	•	Returns Page<TicketResponseDTO>
	•	Acts as agent’s work queue


🔍 Search Tickets API — Short Notes
	•	Implemented GET /tickets/search to search tickets using a keyword.
	•	Supports pagination (page, size) and optional filters (status, priority).
	•	Used JPQL (@Query) with LIKE to search in title + description.
	•	Handled all cases:
	•	keyword only
	•	keyword + status
	•	keyword + priority
	•	keyword + status + priority
	•	If keyword is null/empty → fallback to normal GET tickets API.
	•	Returned results as Page using mapping.
	•	Ensured only authenticated users can access.
